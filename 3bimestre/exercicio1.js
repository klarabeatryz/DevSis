//01) crie uma array chamado cores com três cores diferentes.
//- adicione uma nova cor ao final do array; - remova a primeira cor do array; - mostre o array final no console.
let cores = ['azul', 'lilás', 'amarelo'];

cores.push('rosa');
cores.shift();

console.log(cores);